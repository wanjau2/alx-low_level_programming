#include "main.h"
/**
 * _abs - funtionto find the absolute value of a number
 *
 *@a: The character to prinet number
 * Return: Always 0.
 */
int _abs(int a)
{
	if (a < 0)
		return (-a);
	else
		return (a);
}
